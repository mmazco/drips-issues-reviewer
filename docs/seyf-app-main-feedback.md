# Per-issue feedback

## #32 — Dashboard UI: Real Data Binding & Loading States
- **Grade:** A (13/16, 81%)
- **Milestone:** Dashboard Real-Time Data
- **Real labels:** Stellar Wave
- **Frontmatter labels:** dashboard, frontend, high
- **File paths detected:** 3 (/dashboard, /api/dashboard, /depositar)
- **Checks:** T2 B2 Files2 Scope2 Setup0 AC2 Labels1 Milestone2
- **What's failing:**
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
- **What's in warn:**
  - WARN Labels applied: Only 1 label applied. Add at least one more (e.g. priority + area).

## #31 — Dashboard API: Live Capital, Yield & Advance Data
- **Grade:** B (12/16, 75%)
- **Milestone:** Dashboard Real-Time Data
- **Real labels:** (none)
- **Frontmatter labels:** dashboard, api, backend, high
- **File paths detected:** 1 (/api/dashboard)
- **Checks:** T2 B2 Files1 Scope1 Setup2 AC2 Labels0 Milestone2
- **What's failing:**
  - FAIL Labels applied: No GitHub labels applied. Promote frontmatter labels into real labels (e.g. area:frontend, p1, wave-ready).
- **What's in warn:**
  - WARN File paths referenced: Only 1 file reference found (/api/dashboard). Add at least one more.
  - WARN Scope / area-label stacking: Issue spans 2 areas (dashboard, api, backend, high). Consider splitting.

## #29 — SPEI Off-ramp: Withdrawal Initiation & Status
- **Grade:** B (12/16, 75%)
- **Milestone:** SPEI Off-ramp: Withdrawal Flow
- **Real labels:** Stellar Wave
- **Frontmatter labels:** spei, off-ramp, backend, critical
- **File paths detected:** 1 (/retirar)
- **Checks:** T2 B2 Files1 Scope2 Setup0 AC2 Labels1 Milestone2
- **What's failing:**
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
- **What's in warn:**
  - WARN File paths referenced: Only 1 file reference found (/retirar). Add at least one more.
  - WARN Labels applied: Only 1 label applied. Add at least one more (e.g. priority + area).

## #27 — Soroban Advance Contract: Rules & Buffer Logic
- **Grade:** B (12/16, 75%)
- **Milestone:** Yield Advance Engine (Soroban)
- **Real labels:** Stellar Wave
- **Frontmatter labels:** soroban, smart-contract, blockchain, critical
- **File paths detected:** 1 (/contracts)
- **Checks:** T2 B2 Files1 Scope2 Setup0 AC2 Labels1 Milestone2
- **What's failing:**
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
- **What's in warn:**
  - WARN File paths referenced: Only 1 file reference found (/contracts). Add at least one more.
  - WARN Labels applied: Only 1 label applied. Add at least one more (e.g. priority + area).

## #40 — Simulaciones Monte Carlo: bonos soberanos, fondo ~100k USD y rendimiento Seyf
- **Grade:** B (11/16, 69%)
- **Milestone:** —
- **Real labels:** enhancement
- **Frontmatter labels:** (none)
- **File paths detected:** 1 (/docs)
- **Checks:** T2 B2 Files1 Scope2 Setup0 AC2 Labels1 Milestone1
- **What's failing:**
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
- **What's in warn:**
  - WARN File paths referenced: Only 1 file reference found (/docs). Add at least one more.
  - WARN Labels applied: Only 1 label applied. Add at least one more (e.g. priority + area).
  - WARN Milestone attached: No milestone attached. Assign this issue to its milestone.

## #38 — Observability: Logging, Alerts & Onchain Monitoring
- **Grade:** B (11/16, 69%)
- **Milestone:** Error Handling, Observability & Hardening
- **Real labels:** (none)
- **Frontmatter labels:** observability, devops, medium
- **File paths detected:** 2 (/docs/runbooks/secret-rotation.md, /api/health)
- **Checks:** T2 B2 Files2 Scope1 Setup0 AC2 Labels0 Milestone2
- **What's failing:**
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
  - FAIL Labels applied: No GitHub labels applied. Promote frontmatter labels into real labels (e.g. area:frontend, p1, wave-ready).
- **What's in warn:**
  - WARN Scope / area-label stacking: Issue spans 2 areas (observability, devops, medium). Consider splitting.

## #36 — In-App Async Status Polling & Pending States
- **Grade:** B (11/16, 69%)
- **Milestone:** Notifications & Async Status Updates
- **Real labels:** (none)
- **Frontmatter labels:** frontend, ux, medium
- **File paths detected:** 2 (/depositar, /retirar)
- **Checks:** T2 B2 Files2 Scope1 Setup0 AC2 Labels0 Milestone2
- **What's failing:**
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
  - FAIL Labels applied: No GitHub labels applied. Promote frontmatter labels into real labels (e.g. area:frontend, p1, wave-ready).
- **What's in warn:**
  - WARN Scope / area-label stacking: Issue spans 2 areas (frontend, ux, medium). Consider splitting.

## #34 — History Screen: Real Data & Detail Views
- **Grade:** B (11/16, 69%)
- **Milestone:** Transaction History & State Machine
- **Real labels:** (none)
- **Frontmatter labels:** frontend, history, high
- **File paths detected:** 1 (/historial)
- **Checks:** T2 B2 Files1 Scope2 Setup0 AC2 Labels0 Milestone2
- **What's failing:**
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
  - FAIL Labels applied: No GitHub labels applied. Promote frontmatter labels into real labels (e.g. area:frontend, p1, wave-ready).
- **What's in warn:**
  - WARN File paths referenced: Only 1 file reference found (/historial). Add at least one more.

## #33 — Transaction State Machine & DB Schema
- **Grade:** B (11/16, 69%)
- **Milestone:** Transaction History & State Machine
- **Real labels:** (none)
- **Frontmatter labels:** backend, database, state-machine, high
- **File paths detected:** 2 (/historial, /scripts/migrations/)
- **Checks:** T2 B2 Files2 Scope1 Setup0 AC2 Labels0 Milestone2
- **What's failing:**
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
  - FAIL Labels applied: No GitHub labels applied. Promote frontmatter labels into real labels (e.g. area:frontend, p1, wave-ready).
- **What's in warn:**
  - WARN Scope / area-label stacking: Issue spans 2 areas (backend, database, state-machine, high). Consider splitting.

## #25 — Stellar Wallet Provisioning via Pollar
- **Grade:** B (11/16, 69%)
- **Milestone:** Stellar/Soroban Backend Integration
- **Real labels:** Stellar Wave
- **Frontmatter labels:** stellar, blockchain, pollar, critical
- **File paths detected:** 0
- **Checks:** T2 B2 Files0 Scope2 Setup0 AC2 Labels1 Milestone2
- **What's failing:**
  - FAIL File paths referenced: No file paths mentioned. Add 2+ paths contributors should touch (e.g. app/.../page.tsx, lib/.../file.ts).
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
- **What's in warn:**
  - WARN Labels applied: Only 1 label applied. Add at least one more (e.g. priority + area).

## #20 — Auth Middleware: Protected Routes & JWT Sessions
- **Grade:** B (11/16, 69%)
- **Milestone:** Authentication & Session Management
- **Real labels:** (none)
- **Frontmatter labels:** auth, backend, middleware, critical
- **File paths detected:** 6 (app/app/layout.ts, /app/*, /login, middleware.ts)
- **Checks:** T2 B2 Files2 Scope1 Setup0 AC2 Labels0 Milestone2
- **What's failing:**
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
  - FAIL Labels applied: No GitHub labels applied. Promote frontmatter labels into real labels (e.g. area:frontend, p1, wave-ready).
- **What's in warn:**
  - WARN Scope / area-label stacking: Issue spans 2 areas (auth, backend, middleware, critical). Consider splitting.

## #42 — Advance Simulator: Interactive Amount Picker & Confirmation Sheet
- **Grade:** C (10/16, 63%)
- **Milestone:** Frontend Polish & UX Hardening
- **Real labels:** (none)
- **Frontmatter labels:** frontend, ux, advance, high
- **File paths detected:** 7 (app/app/adelanto/page.ts, components/app/advance-simulator.ts, components/app/dashboard-client.ts, /adelanto)
- **Checks:** T2 B2 Files2 Scope0 Setup0 AC2 Labels0 Milestone2
- **What's failing:**
  - FAIL Scope / area-label stacking: Issue stacks 3 area concerns (frontend, ux, advance, high). Split into single-area tickets.
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
  - FAIL Labels applied: No GitHub labels applied. Promote frontmatter labels into real labels (e.g. area:frontend, p1, wave-ready).

## #41 — Onboarding Flow: Multi-Step Registration & Empty States
- **Grade:** C (10/16, 63%)
- **Milestone:** Frontend Polish & UX Hardening
- **Real labels:** (none)
- **Frontmatter labels:** frontend, ux, onboarding, high
- **File paths detected:** 11 (app/registro/page.ts, app/login/page.ts, components/app/dashboard-client.ts, /registro)
- **Checks:** T2 B2 Files2 Scope0 Setup0 AC2 Labels0 Milestone2
- **What's failing:**
  - FAIL Scope / area-label stacking: Issue stacks 3 area concerns (frontend, ux, onboarding, high). Split into single-area tickets.
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
  - FAIL Labels applied: No GitHub labels applied. Promote frontmatter labels into real labels (e.g. area:frontend, p1, wave-ready).

## #35 — Push / SMS Notification Service Integration
- **Grade:** C (10/16, 63%)
- **Milestone:** Notifications & Async Status Updates
- **Real labels:** Stellar Wave
- **Frontmatter labels:** notifications, backend, medium
- **File paths detected:** 0
- **Checks:** T2 B2 Files0 Scope1 Setup0 AC2 Labels1 Milestone2
- **What's failing:**
  - FAIL File paths referenced: No file paths mentioned. Add 2+ paths contributors should touch (e.g. app/.../page.tsx, lib/.../file.ts).
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
- **What's in warn:**
  - WARN Scope / area-label stacking: Issue spans 2 areas (notifications, backend, medium, Stellar Wave). Consider splitting.
  - WARN Labels applied: Only 1 label applied. Add at least one more (e.g. priority + area).

## #30 — Off-ramp Reconciliation & Webhook Handling
- **Grade:** C (10/16, 63%)
- **Milestone:** SPEI Off-ramp: Withdrawal Flow
- **Real labels:** (none)
- **Frontmatter labels:** spei, reconciliation, backend, high
- **File paths detected:** 0
- **Checks:** T2 B2 Files0 Scope2 Setup0 AC2 Labels0 Milestone2
- **What's failing:**
  - FAIL File paths referenced: No file paths mentioned. Add 2+ paths contributors should touch (e.g. app/.../page.tsx, lib/.../file.ts).
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
  - FAIL Labels applied: No GitHub labels applied. Promote frontmatter labels into real labels (e.g. area:frontend, p1, wave-ready).

## #28 — Advance Engine API: Simulate & Confirm Endpoints
- **Grade:** C (10/16, 63%)
- **Milestone:** Yield Advance Engine (Soroban)
- **Real labels:** Stellar Wave
- **Frontmatter labels:** advance, backend, api, critical
- **File paths detected:** 1 (/adelanto)
- **Checks:** T2 B2 Files1 Scope0 Setup0 AC2 Labels1 Milestone2
- **What's failing:**
  - FAIL Scope / area-label stacking: Issue stacks 3 area concerns (advance, backend, api, critical, Stellar Wave). Split into single-area tickets.
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
- **What's in warn:**
  - WARN File paths referenced: Only 1 file reference found (/adelanto). Add at least one more.
  - WARN Labels applied: Only 1 label applied. Add at least one more (e.g. priority + area).

## #26 — MXNe Bridge & Stablebonds Auto-Deployment
- **Grade:** C (10/16, 63%)
- **Milestone:** Stellar/Soroban Backend Integration
- **Real labels:** (none)
- **Frontmatter labels:** stellar, etherfuse, stablebonds, critical
- **File paths detected:** 0
- **Checks:** T2 B2 Files0 Scope2 Setup0 AC2 Labels0 Milestone2
- **What's failing:**
  - FAIL File paths referenced: No file paths mentioned. Add 2+ paths contributors should touch (e.g. app/.../page.tsx, lib/.../file.ts).
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
  - FAIL Labels applied: No GitHub labels applied. Promote frontmatter labels into real labels (e.g. area:frontend, p1, wave-ready).

## #24 — Deposit Ledger & Auto-Deploy Trigger
- **Grade:** C (10/16, 63%)
- **Milestone:** SPEI Deposit: CLABE & Webhooks
- **Real labels:** (none)
- **Frontmatter labels:** spei, blockchain, backend, critical
- **File paths detected:** 0
- **Checks:** T2 B2 Files0 Scope2 Setup0 AC2 Labels0 Milestone2
- **What's failing:**
  - FAIL File paths referenced: No file paths mentioned. Add 2+ paths contributors should touch (e.g. app/.../page.tsx, lib/.../file.ts).
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
  - FAIL Labels applied: No GitHub labels applied. Promote frontmatter labels into real labels (e.g. area:frontend, p1, wave-ready).

## #23 — SPEI Inbound: Virtual CLABE Generation & Webhook
- **Grade:** C (10/16, 63%)
- **Milestone:** SPEI Deposit: CLABE & Webhooks
- **Real labels:** (none)
- **Frontmatter labels:** spei, payments, backend, critical
- **File paths detected:** 1 (/depositar)
- **Checks:** T2 B2 Files1 Scope1 Setup0 AC2 Labels0 Milestone2
- **What's failing:**
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
  - FAIL Labels applied: No GitHub labels applied. Promote frontmatter labels into real labels (e.g. area:frontend, p1, wave-ready).
- **What's in warn:**
  - WARN File paths referenced: Only 1 file reference found (/depositar). Add at least one more.
  - WARN Scope / area-label stacking: Issue spans 2 areas (spei, payments, backend, critical). Consider splitting.

## #19 — Auth API: Phone/Email OTP Registration
- **Grade:** C (10/16, 63%)
- **Milestone:** Authentication & Session Management
- **Real labels:** (none)
- **Frontmatter labels:** auth, backend, critical
- **File paths detected:** 1 (/registro)
- **Checks:** T2 B2 Files1 Scope1 Setup0 AC2 Labels0 Milestone2
- **What's failing:**
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
  - FAIL Labels applied: No GitHub labels applied. Promote frontmatter labels into real labels (e.g. area:frontend, p1, wave-ready).
- **What's in warn:**
  - WARN File paths referenced: Only 1 file reference found (/registro). Add at least one more.
  - WARN Scope / area-label stacking: Issue spans 2 areas (auth, backend, critical). Consider splitting.

## #22 — KYC Review State Machine & Deposit Limit Unlock
- **Grade:** C (9/16, 56%)
- **Milestone:** KYC / Merchant Onboarding
- **Real labels:** (none)
- **Frontmatter labels:** kyc, backend, critical
- **File paths detected:** 0
- **Checks:** T2 B2 Files0 Scope1 Setup0 AC2 Labels0 Milestone2
- **What's failing:**
  - FAIL File paths referenced: No file paths mentioned. Add 2+ paths contributors should touch (e.g. app/.../page.tsx, lib/.../file.ts).
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
  - FAIL Labels applied: No GitHub labels applied. Promote frontmatter labels into real labels (e.g. area:frontend, p1, wave-ready).
- **What's in warn:**
  - WARN Scope / area-label stacking: Issue spans 2 areas (kyc, backend, critical). Consider splitting.

## #21 — KYC Data Collection Flow & Document Upload
- **Grade:** C (9/16, 56%)
- **Milestone:** KYC / Merchant Onboarding
- **Real labels:** (none)
- **Frontmatter labels:** kyc, onboarding, backend, critical
- **File paths detected:** 1 (/identidad)
- **Checks:** T2 B2 Files1 Scope0 Setup0 AC2 Labels0 Milestone2
- **What's failing:**
  - FAIL Scope / area-label stacking: Issue stacks 3 area concerns (kyc, onboarding, backend, critical). Split into single-area tickets.
  - FAIL Setup / validation steps: No code block or shell command found. Add a "Suggested execution" block with fork/branch commands, how to run the code, and how to validate (e.g. `npm run lint`, `npx tsc --noEmit`).
  - FAIL Labels applied: No GitHub labels applied. Promote frontmatter labels into real labels (e.g. area:frontend, p1, wave-ready).
- **What's in warn:**
  - WARN File paths referenced: Only 1 file reference found (/identidad). Add at least one more.

